# Copy to .env.local and adjust. Restart `npm run dev` after changes.
# This app now uses built-in Next.js API routes for optimization.
# Optional free HF token for better rate limits:
# HUGGINGFACEHUB_API_TOKEN=hf_xxx
