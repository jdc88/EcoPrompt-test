{
  "name": "ecoprompt",
  "private": true,
  "scripts": {
    "dev": "WATCHPACK_POLLING=true npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
